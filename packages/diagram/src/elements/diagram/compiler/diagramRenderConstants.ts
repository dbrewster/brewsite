/** @deprecated Import from '../constants' directly. This shim will be removed. */
// Backwards-compatibility shim — re-exports from element-level constants.
export { GROUP_BORDER_PX_TO_UNITS, GROUP_RENDER_Z, NODE_RENDER_Z_OFFSET } from '../constants';
