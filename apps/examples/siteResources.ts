// Asset manifest for the @brewsite/apps examples sub-app.
// Examples currently use only <Diagram> elements (no GLTF models),
// so the models array is empty. Add entries here when model-based
// examples are introduced.
export const siteResources = {
  models: [],
} as const;
