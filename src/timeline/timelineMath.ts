// Re-export from math.ts for compatibility
export { clamp01, lerp, invLerp, rangeProgress, smoothstep, createRangeTransition, createFadeTransition } from './math';
export type { EaseFn } from './math';
